{
  "name": "FileUploaderButton",
  "level": 2,
  "status": "stable",
  "category": "carbon",
  "summary": "Carbon FileUploaderButton.",
  "variants": {
    "buttonKind": [
      "primary",
      "secondary",
      "danger",
      "ghost",
      "danger--primary",
      "danger--ghost",
      "danger--tertiary",
      "tertiary"
    ],
    "size": [
      "sm",
      "small",
      "field",
      "md",
      "lg"
    ]
  },
  "props": [
    {
      "name": "accept",
      "required": false,
      "type": "string[]",
      "description": "Specify the types of files that this input should be able to receive"
    },
    {
      "name": "buttonKind",
      "required": false,
      "type": "'primary' | 'secondary' | 'danger' | 'ghost' | 'danger--primary' | 'danger--ghost' | 'danger--tertiary' | 'tertiary'",
      "description": "Specify the type of underlying button"
    },
    {
      "name": "disableLabelChanges",
      "required": false,
      "type": "boolean",
      "description": "Specify whether you want to disable any updates to the FileUploaderButton label"
    },
    {
      "name": "disabled",
      "required": false,
      "type": "boolean",
      "description": "Specify whether file input is disabled"
    },
    {
      "name": "innerRef",
      "required": false,
      "type": "React.RefObject<HTMLLabelElement | null>",
      "description": null
    },
    {
      "name": "labelText",
      "required": false,
      "type": "React.ReactNode",
      "description": "Provide the label text to be read by screen readers when interacting with this control"
    },
    {
      "name": "multiple",
      "required": false,
      "type": "boolean",
      "description": "Specify if the component should accept multiple files to upload"
    },
    {
      "name": "name",
      "required": false,
      "type": "string",
      "description": "Provide a name for the underlying `<input>` node"
    },
    {
      "name": "size",
      "required": false,
      "type": "'sm' | 'small' | 'field' | 'md' | 'lg'",
      "description": "Specify the size of the FileUploaderButton, from a list of available sizes."
    }
  ],
  "tokens_used": [
    "semantic.*"
  ],
  "a11y": {
    "contrast": "WCAG 2.2 AA — inherited from Carbon",
    "note": "Carbon ships a11y behaviour per component; verify per screen"
  },
  "when_to_use": "Use Carbon FileUploaderButton rather than building one.",
  "when_not_to_use": "Do not restyle off-token — propose a token instead.",
  "source": "@carbon/react@1.115.0 (package/es/components/FileUploader/FileUploaderButton.d.ts) — Apache-2.0",
  "parse": {
    "interface": "FileUploaderButtonProps",
    "kind": "full",
    "partial": false,
    "prop_count": 9,
    "variant_count": 2
  }
}
