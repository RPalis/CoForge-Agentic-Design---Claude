{
  "name": "TabsVertical",
  "level": 2,
  "status": "stable",
  "category": "carbon",
  "summary": "Carbon TabsVertical.",
  "variants": {},
  "props": [
    {
      "name": "defaultSelectedIndex",
      "required": false,
      "type": "number",
      "description": "Specify which content tab should be initially selected when the component is first rendered"
    },
    {
      "name": "height",
      "required": false,
      "type": "string",
      "description": "Option to set a height style only if using vertical variation"
    },
    {
      "name": "selectedIndex",
      "required": false,
      "type": "number",
      "description": "Provide an optional function which is called whenever the state of the `Tabs` changes / onChange?(state: { selectedIndex: number; }): void; /** Control which content panel is currently selected. This puts the component in a controlled mode and should be used along with `onChange`"
    }
  ],
  "tokens_used": [
    "semantic.*"
  ],
  "a11y": {
    "contrast": "WCAG 2.2 AA — inherited from Carbon",
    "note": "Carbon ships a11y behaviour per component; verify per screen"
  },
  "when_to_use": "Use Carbon TabsVertical rather than building one.",
  "when_not_to_use": "Do not restyle off-token — propose a token instead.",
  "source": "@carbon/react@1.115.0 (package/es/components/Tabs/Tabs.d.ts) — Apache-2.0",
  "parse": {
    "interface": "TabsVerticalProps",
    "kind": "full",
    "partial": false,
    "prop_count": 3,
    "variant_count": 0
  }
}
