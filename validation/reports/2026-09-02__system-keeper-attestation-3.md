# Independent attack on the four validation fixes — ATTESTATION REFUSED

**Date:** 2026-09-02 · **Agent:** system-keeper (independent; did not author the changes)
**Subject:** C-026, C-027 (E-2), C-028, C-029 · **Author's account:**
`validation/reports/2026-09-02__system-keeper-four-fixes.md`

> **I am not attesting.** The machinery hash is deliberately **omitted** from this report,
> so check 5g will continue to error — which is the correct outcome. Three checks are
> narrower than the claims recorded for them, and two of those claims are sitting in
> `validation/coverage.json` marked **verified**. That is the C-021 / C-024 pattern this
> ritual exists to catch, and it is the one thing an attester may not wave through.

**This is not a rejection of the work.** All four fixes are net improvements on what they
replaced, every check fires on the defect it was built for, and nothing regressed. The
refusal is about the distance between what the checks do and what the ledger says they do.
Everything below is small and specific; §7 lists exactly what would earn attestation.

**Severity counts: 3 error · 3 warning · 2 info.**

---

## Method

Everything was verified by running it and by planting the fault, never by reading code.
Baseline sha256 of all 507 non-`.git` files taken before any probe and re-taken after;
`git status --short` diffed against its session-start snapshot.

| regression | result |
|---|---|
| `test-gates.py` | **17 passed · 0 failed** |
| `audit-system.py` baseline | 0 blocker · 1 error (5g) · 2 warning · 0 skipped — reproduces the author's numbers exactly |
| `design-system/component-index.json` | `a58e7b68d651bb70bd313e99cc57975afe01c48626a7a3fe95a1b0e1ad90867f` — unchanged, matches the author's recorded value |
| `validation/corrections.json` | `e38bb16501dd8dafdac4ffa78177c5134f1870e1553705b47db300ea96d54004` — unchanged |
| `validation/published-surfaces.json` | `bd572dc47643a2bfec3e517e19e4493401d67b7bcf1d9fd5ce82e772e27f3866` — unchanged, matches the author's recorded value |
| `.ai/index.json` / `index.md` | unchanged; `index-system.py` confirmed idempotent over two extra runs |
| `git status --short` | byte-identical to session start |

Only `validation/metrics/2026-09-02.json` and `METRICS.md` differ from my baseline, and
they are regenerated by the Stop hook on every turn end regardless of what any agent does
(see F-8 and I-2).

---

## F-1 · ERROR · The 500-byte floor is defeated by one shell line containing no prose

The author's claim, verbatim: *"every remaining bypass now requires deliberately composing
500 bytes of prose that is not true."* That is false. The flag added to close the bypass
supplies the token, and the audit's own output supplies the bulk:

```
(python3 validation/audit-system.py --machinery-hash; python3 validation/audit-system.py) \
  > validation/reports/2026-09-02__token-keeper-audit.md 2>&1
```

Observed: **3,613 bytes, hash present once, filename matches the roster filter.** Check 5g
flipped from `[ERROR] … no audit report attests` to
`[INFO] attestation: machinery changed to …; attested by 2026-09-02__token-keeper-audit.md`.
A pure-filler variant also passed at 718 bytes:
`{ audit --machinery-hash; python3 -c "print('filler '*100)"; } > …__brand-director-audit.md`.

Both files were deleted and the ERROR confirmed to return.

**What the fix does achieve, stated fairly.** The author's negative tests all reproduce:
the plain redirect now contains the hash **0 times**, `--json` and `--report` leak it 0
times, and the 17-byte hash-only file is rejected loudly by name. The residual gain is real
but is *conspicuousness*, not cost: `audit > report.md` looked like a normal thing to do,
whereas concatenating two invocations is visibly contrived in a shell history.

**Why it is an error anyway.** The recorded strength claim is not the one the mechanism
delivers, and it is the second time in this repository that a hardening was described as
stronger than it is. Before: one command line, no prose. After: one command line, no prose.
The floor is doing none of the work attributed to it.

## F-2 · ERROR · Check 5h can be silenced on a genuinely stale page, and V-021 says it cannot

This answers the author's own question 2. I took the one page 5h correctly flags — `CoForge
Agentic Design`, genuinely the stalest surface tracked — and changed `documents` from
`{"asserted_state_date": "2026-08-28"}` to `null`, with a 56-character note:

> `"Historical snapshot; nothing here tracks repository state."`

The warning vanished and was replaced by
`[INFO] surfaces: 2 of 2 versioned published surfaces are current; 4 declare nothing that can go stale`.
The board went green on an unchanged, still-stale page. Ledger restored byte-identical.

`documents: null` is an exemption available on demand: the only cost is 20 characters of
plausible prose, and nothing verifies the prose is true. The author's *code comment*
anticipates this (*"or it is just an untracked page wearing an exemption"*), but the report's
**"Limit, stated"** section names only the absence of a content check, and
`validation/coverage.json` **V-021** asserts:

> *"A page published outside this repository cannot fall behind the state it documents
> without the audit saying so"* — `verified_by: "validation/audit-system.py"`

It can, in one field edit. Per rule 5 that row is not verified.

**Confirmed working:** the stale-version warning, the ahead-of-repo error, the uncomparable
version error and the malformed-entry errors all fire as documented.

## F-3 · ERROR · Check 5i validates a self-declared label, not provenance — and V-022 says otherwise

This answers the brief's question directly: **yes**, a metrics file can be made to pass
while carrying fabricated counts. I replaced today's `gates` block with a perfect green
board and kept the marker:

```json
{"verdict":"PASS","blocker":0,"error":0,"warning":0,"info":99,"skipped":0,"source":"live-audit"}
```

Result: `[INFO] metrics: 2026-09-02.json: gate counts derived from a live audit run`. 5i
compares one string that the file asserts about itself; nothing ties the counts to any run.

**Worse, the failure path sets the same marker.** `from_audit()` returns
`"source": "live-audit"` unconditionally, including in the `except` branch. I moved
`audit-system.py` aside and ran `collect-metrics.py`:

- the record honestly showed `verdict: UNAVAILABLE` with **null** counts, stderr carried
  the warning, `METRICS.md` rendered `—`, exit 0, no crash — **the author's core C-026
  claim holds and is the strongest single piece of this pass**;
- but the audit then reported `[INFO] metrics: … gate counts derived from a live audit run`
  over a record where **no audit ran at all**.

So 5i catches the *old* shape of C-026 (marker absent) and passes its exact equivalent
(marker present, nothing behind it). `coverage.json` **V-022** — *"A metrics run record's
gate counts describe the run that produced them"*, `verified_by: validation/audit-system.py`
— is not verified.

**Confirmed working:** the unparseable-record branch fires as
`[ERROR] metrics: validation/metrics/2026-09-02.json is unreadable`.

## F-4 · WARNING · A published page absent from the ledger is undetectable, and the one checkable direction is unchecked

Asked plainly by the brief; answered plainly. **No.** 5h iterates
`published-surfaces.json` and nothing else. There is no enumeration of published pages
anywhere in the repository, and four of the six ledger entries have `artifact: null`, so for
those the ledger is only as good as whoever remembered to add a row. Nothing can change that
from inside CI.

But one direction **is** checkable today and is not checked: two artifact manifests declare
a real `surface.ref`
(`2026-08-27__brand-extraction__coforge-web__v1`, `2026-08-27__competitive-benchmark__body-face__v1`).
I cross-checked them — both are in the ledger, so there is no live inconsistency — but no
check enforces it, so a manifest can declare a published surface the ledger never learns
about. Per rule 6 this belongs inside 5h, which already asks this question. The same
manifest also duplicates `documents_tokens_version: "0.2.0"`, which the ledger states
separately, with nothing checking the two agree.

## F-5 · WARNING · C-029: a vendor row can wrongly flip the fork; confirmed by planting

The brief's harder question. `_l2_authorship()` requires `\bCoForge\b` **and** no
`@pkg@version` spec. Three plants into `component-index.json`, each restored and re-verified
byte-identical:

| planted `source` on an L2 row | result |
|---|---|
| `CoForge L2 component — authored here, promoted by ADR-999` | `REVIEW`, ours 1 / vendor 208 — author's claim reproduces exactly |
| `Ingested by the CoForge Carbon adapter from Carbon React 1.115.0 — Apache-2.0` | **`REVIEW`, ours 1** — a vendor row wrongly counted as authored |
| `Authored in-house, promoted by ADR-030 through the membrane` | `RED`, vendor 209 — a genuinely authored row missed |

The false flip is real: any rephrasing of the adapter's `source` string that mentions CoForge
without a `pkg@version` spec — *"ingested by the CoForge adapter"* is an entirely natural
thing to write — flips all 208 rows and the fork with them.

**Held down to warning** because the failure direction is safe: the generator emits `REVIEW`,
never `YELLOW`/`GREEN`, so the worst outcome is a spurious prompt to a human, never an
automatic promotion. That design choice is correct and I endorse it. V-023 discloses the
adjacent case (a hand-written `source` naming CoForge) but not this one, and the fix is a
negative marker on the vendor side, not a wider positive one.

**Confirmed working:** derivation is a pure function of the index, `.ai/` is idempotent, and
the CI step named as C-029's check genuinely exists —
`.github/workflows/ci.yml` line 31-35 rebuilds `.ai/` and runs `git diff --exit-code .ai/`.

## F-6 · WARNING · `check` is validated by file existence only, and three of these four closures lean on it

`audit-system.py` line 521 is `elif not os.path.exists(P(chk))`. I planted a correction
naming `validation/does-not-exist.py` and it correctly produced
`[BLOCKER] corrections: C-999 names check 'validation/does-not-exist.py', which does not exist`
— rule 3's guard works.

But C-026, C-027 and C-028 all name `validation/audit-system.py`, a ~900-line file. Deleting
check 5h or 5i from it would leave all three corrections pointing at a file that still
exists, and nothing would notice. "Found and fixed is two of three" is currently satisfied by
naming a file, with the actual guarantee carried in the `verifies` prose. Pre-existing, not
introduced here — but these four fixes make it load-bearing for three more entries, and it is
the same shape as the C-024 finding the brief cites (a claim closed by naming something that
does not actually verify it).

## F-7 · INFO · The honesty calls are right, and I would not have made them differently

Asked to judge whether V-019/V-020 are rigour or deferral dressed up as rigour: **rigour.**

- C-027's `verifies` field opens with *"PARTIAL — E-2 ONLY, and the other three are still
  open. Read this before treating C-027 as closed."* Confirmed in the ledger.
- V-019 and V-020 carry `verified_by: null` and are reported every run — the audit's warning
  names them: `coverage: 3 of 23 load-bearing claims are UNVERIFIED: V-015, V-019, V-020`.
- Each remaining C-027 defect changes what 5g accepts, which CLAUDE.md puts behind Gate A.
  Bundling them into a self-authored pass would have been the wrong call.

Moving them from `attestation.json` into `coverage.json` makes them *more* visible, not less;
a `verified_by: null` row that fires a warning on every audit is a stronger position than a
closed correction. This part of the work is exemplary. It is precisely why F-2 and F-3 matter:
the same author applied the correct standard to V-019/V-020 and not to V-021/V-022.

## F-8 · INFO · The Stop hook regenerates the metrics record every turn — an interaction the report does not consider

`.claude/hooks/session-check.py` lines 24-27 run `collect-metrics.py` at every turn end.
Consequences worth recording:

- `validation/metrics/<today>.json` is not a deliberate daily collection; it is a snapshot of
  the last instant of the last turn, and it will be rewritten again after this report.
- Now that `from_audit()` runs the audit **live**, any agent probing with planted faults has
  those transient counts baked into the day's record and stamped `source: "live-audit"` —
  truthfully, and misleadingly. This is a new hazard created by an otherwise correct fix, and
  it plausibly explains the `error: 2` in the committed record against a live board of 1.
- I checked the timeout risk and it is **not** a problem: `collect-metrics.py` now runs a full
  audit inside itself, but measures 0.27s against the hook's 90s cap (audit alone: 0.12s). No
  finding.

---

## Disclosure — my own error during this pass

While backing files up I used `metrics.bak` and `METRICS.bak` as two names on a
case-insensitive filesystem; they are one file. My restore therefore wrote `METRICS.md`'s
contents over `validation/metrics/2026-09-02.json`. I did not notice until a sha256 check
failed, which is the reason the check was there.

The damage was self-repairing: the Stop hook (F-8) regenerated both files, and the current
record — `{"verdict":"FAIL","blocker":0,"error":1,"warning":2,"info":7,"skipped":0,
"source":"live-audit"}` with `corrections {logged 29, with_check 29, unchecked 0}` — matches
the live board. The author's exact byte-content of that record is not recoverable, and it
would have been overwritten by the next turn regardless. Recorded because a restore that
silently failed is exactly what I was sent to look for in someone else's work.

---

## §7 · What would earn an attestation

None of this requires redesign. In rough order of importance:

1. **Withdraw or narrow the F-1 claim.** Either state in the correction and the report that
   the floor buys conspicuousness rather than cost, or make 5g's acceptance require something
   a redirect cannot produce. Do not leave *"requires composing 500 bytes of prose that is not
   true"* on the record.
2. **`V-021.verified_by → null`**, or narrow the claim to what 5h checks: *"a page whose
   `documents` declares a comparable version cannot fall behind it unnoticed."* Add the
   `documents: null` exemption to the report's stated limits and to `known_limits`.
3. **`V-022.verified_by → null`**, or narrow it to *"a run record declares where its gate
   counts came from."* Separately, stop returning `source: "live-audit"` from the failure
   branch of `from_audit()` — `"live-audit-unavailable"` costs one string and makes 5i able
   to tell the two apart.
4. **Extend 5h** (not a new check) to compare manifest `surface.ref` values against the
   ledger, and state plainly that ledger completeness is unverifiable for pages with no
   manifest.
5. **Record the F-5 vendor-side false flip** in V-023 alongside the authored-side gap.

Items 1-3 are prose and one string literal. On seeing them, I would attest.

---

## Restoration

Every planted fault was removed and verified by hash. `git status --short` is byte-identical
to its session-start snapshot apart from this report. `design-system/tokens/tokens.json`,
`design-system/component-index.json`, `validation/adapters/`, `research/sources/` and
`validation/attestation.json` were not modified.
